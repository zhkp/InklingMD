# InklingMD

一款「所见即所得」的本地 Markdown 编辑器，对标 Typora。基于 Tauri 2 + React 19 + Milkdown 构建，编辑与预览融为一体——无需左右分栏、无需切换模式，输入 Markdown 语法后立即渲染成富文本，底层保存的始终是标准 Markdown 纯文本。

[![Build](https://github.com/zhkp/InklingMD/actions/workflows/build.yml/badge.svg?branch=main)](https://github.com/zhkp/InklingMD/actions/workflows/build.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](https://opensource.org/licenses/MIT)

## 功能特性

### 编辑器内核
- 实时所见即所得渲染（基于 Milkdown / ProseMirror）
- **源代码模式**：整页切换为 CodeMirror 6 编辑原始 Markdown（GFM 语法高亮 + 行号，`Ctrl/Cmd+Alt+S` 切换，按标签页记忆），内置查找/替换面板（`Ctrl/Cmd+F` / `Ctrl/Cmd+R`），退出时自动重置撤销历史并尽量保持光标/滚动位置
- 支持标题、加粗、斜体、删除线、行内代码、引用块、分割线
- 有序/无序/任务列表，多级嵌套（Tab/Shift+Tab 缩进）
- GFM 表格，附表格工具栏（增删行列、对齐、快速插入）
- 围栏代码块 + 语法高亮（CodeMirror，覆盖主流语言，可切换主题）
- KaTeX 数学公式（行内 `$...$` 与块级 `$$...$$`，支持 mhchem 化学方程式、自动编号；工具栏 `$ 行内` / `∑ 公式` 按钮与斜杠菜单 `/行内` / `/公式` 可直接插入空公式节点并自动进入 LaTeX 编辑态，双击已有公式可重新编辑）
- Mermaid 图表（流程图、时序图、甘特图等），支持「下载」按钮导出 SVG、`Ctrl/Cmd+滚轮`缩放图表（0.5~3x），放大后可按住鼠标拖动平移查看各区域，双击重置缩放与平移
- **HTML 嵌入**：白名单渲染 `<span style="color:red">`、`<kbd>`、`<mark>`、`<details>`、`<blockquote>` 等 HTML 标签，过滤 script/on*/javascript: 等危险内容
- **YAML Front Matter**：文档首部 `---` 围栏块，内嵌 CodeMirror 编辑 YAML
- **脚注**：标准 GFM 脚注语法 `[^1]` 与 `[^1]: 内容`，点击互相跳转
- **`[TOC]` 目录**：在文档任意位置写 `[TOC]`，自动根据标题生成目录树，点击跳转
- **callout 提示框**：支持 `> [!NOTE/WARNING/TIP/IMPORTANT]` 等 GFM 提示框语法，带图标配色
- **斜杠菜单**：空行输入 `/` 弹出块类型选择菜单，键盘导航 + 模糊过滤
- **自动配对补全**：输入括号 / 引号自动配对，光标置中；选中文字输入符号则包裹（含中文引号 / 书名号，可在设置开关）
- **图片缩放 / 对齐**：图片右下角拖拽缩放，右键设置对齐（左/中/右），属性编码进 markdown title 持久化
- **行内图片**：图片在文字流中行内显示
- **拖拽块排序**：每个顶层块左侧悬停出现 ⋮⋮ 手柄，按住拖动整块重排，drop 指示器高亮目标位置

### 文件与工作区
- **多标签页编辑**：同时打开多个 `.md` 文件，标签页切换，未保存提示与关闭确认
- **标签页右键菜单 + 拖拽重排**：关闭/关闭其他/关闭右侧/全部关闭/复制路径/在分屏打开/在新窗口打开，拖拽调整顺序
- **多面板分屏**：标签页右键「在分屏打开」启动右侧第二面板，双编辑器实例独立编辑两个文件，支持左右交换（多窗口对照编辑场景）
- **多窗口**：文件树或标签页右键「在新窗口打开」，Tauri 多窗口在独立窗口打开文件（多显示器场景）
- 文件树侧边栏：以文件夹为单位打开工作区（`Ctrl/Cmd+\` 切换显隐）
- **单文件模式**：「打开文件」直接打开散落在不同文件夹的 `.md`，不绑定工作区，多个文件作为标签页并存
- **文件树操作**：右键重命名、删除、新建文件/文件夹（含未保存提示与确认）
- **最近打开文件列表**：侧边栏顶部列出最近 10 个文件，点击直达（重启保留）
- **快速打开（Quick Open）**：`Ctrl/Cmd+P` 弹出面板，输入即过滤（匹配文件名 / 路径，支持子序列），`↑`/`↓` 选择、`Enter` 打开、`Esc` 关闭；排序优先「已打开 → 最近打开 → 根目录附近 → 匹配度更高」，键盘全程可用（combobox 语义 + 焦点陷阱）。索引在工作区级缓存（TTL 30 秒 + 过期先出旧结果再后台重建），重命名 / 删除 / 另存为后自动失效，5000 文件工作区首次遍历约 20ms
- **编辑位置记忆**：关闭文件时记住光标与滚动位置，重新打开时恢复
- **文件夹折叠状态记忆**：记住侧边栏每个文件夹的展开/折叠状态，重启恢复
- **书签 / 收藏**：文件右键加入书签，侧边栏书签区块列出所有书签，点击跳转（重启保留）
- **大纲面板**：根据标题生成目录树，点击跳转，当前标题自动高亮（`Ctrl/Cmd+'` 切换显隐）
- 自动保存（防抖 2 秒）+ `Ctrl/Cmd+S` 手动保存
- **外部修改监听与冲突处理**：检测到磁盘文件被其它程序修改（如 Git 切分支、网盘同步）时提示重新加载；本地有未保存修改时弹出冲突对话框，可「保留本地另存副本（.backup.md）/ 查看行级差异对比 / 丢弃本地修改重载 / 继续编辑」，避免静默覆盖外部修改导致数据丢失
- 图片拖拽 / 粘贴自动保存到当前 Markdown 文件同目录的 `assets` 目录
- 链接跟随：`Ctrl/Cmd+点击` 打开链接

### 搜索
- **查找替换**（当前文件）：支持正则表达式、区分大小写、上一个/下一个导航；源码模式下自动切换为 CodeMirror 内置查找/替换面板
- **全局搜索**：跨工作区所有 `.md` 文件搜索（`Ctrl/Cmd+Shift+F`），按文件分组展示命中，点击跳转到对应行；忽略规则与文件树统一，并遵循工作区 `.gitignore`（`node_modules`、`venv`、`vendor` 等不再出现在结果里）

### 导出与复制
- 导出 HTML（含内嵌样式）
- 导出 PDF（调用浏览器打印）
- **导出 Word（.docx）**：通过本地 Pandoc 转换，支持图文与基础样式（需本地预先安装 Pandoc，详见 [Pandoc 官方下载](https://pandoc.org/installing.html)；若未安装会在导出时弹出友好的安装指引）
- **导出长图（PNG）**：用 html2canvas 把整篇文档渲染成 2x 高清 PNG 长图，离屏容器与主编辑器样式/主题严格对齐并自动等待异步图片就绪，方便分享到社交平台
- **导出大纲**：只导出标题层级，生成带缩进列表 + 原始标题结构的 md 文件，当目录用
- 复制为富文本（粘贴到其它软件保留样式）
- 复制为纯 Markdown 源码

### 样式与主题
- 明暗模式切换
- 支持加载自定义 CSS 覆盖样式（CSS 变量）
- 代码块语法高亮主题独立可配置（One Dark / 浅色 / 无高亮）
- **设计令牌系统**：全应用通过 CSS 变量统一管理配色 / 阴影 / 圆角 / 动效曲线，主题切换与调色只改一处
- **统一 SVG 图标库**：线性风格（`stroke = currentColor`），随文字颜色继承，跨平台渲染一致
- **现代化交互细节**：细半透明滚动条、`:focus-visible` 键盘聚焦环、菜单 / 模态弹入动效、ghost 风格顶栏按钮、活跃 tab 卡片样式 + 强调色指示条、活跃文件左侧指示条、iOS 风格 Toggle 开关、模态毛玻璃遮罩、渐变品牌标题

### 辅助写作
- 状态栏字数 / 字符数 / 行数 / 预计阅读时长统计
- **文档缩放**：`Ctrl/Cmd+滚轮` 等比放大/缩小整个文档（50%~300%），`Ctrl/Cmd+0` 重置 100%，状态栏显示当前百分比可点击重置，缩放级别跨会话持久化
- **专注模式**：弱化非当前段落，聚焦当前内容
- **打字机模式**：当前编辑行始终保持在视窗垂直居中
- **禅模式**：`F11` 隐藏所有 UI（侧边栏/大纲/标签页/工具栏/状态栏），纯编辑，`Esc` 退出
- **拼写检查**：浏览器原生拼写检查（红波浪线 + 右键修正建议），偏好设置开关（默认关闭）
- **偏好设置面板**：集中开关各项编辑器行为（`Ctrl/Cmd+,` 打开）
- **错误边界**：编辑器渲染异常时显示降级 UI 而非白屏，内容不丢失

### 性能优化
- **插件回调守卫**：formula-numbering / block-drag / outline 等插件加 `docChanged` 守卫，消除纯光标移动时的全树遍历
- **cursor-saver 防抖**：光标位置本地缓存 + 300ms 防抖落 store，避免每次移动触发 TabsBar / useAutoSave 全局重渲染
- **精准订阅**：TabsBar / useAutoSave 改为订阅派生快照，打字时 UI 不重渲染
- **代码块懒挂载**：CodeMirror 实例延迟到代码块进入视口（200px 预加载）时才创建，大量代码块文档首屏开销显著下降
- **Mermaid 视口懒渲染 + 空闲预渲染 + 渲染缓存**：打开时只渲染视口内图表（300px 预载边距），视口外图表由 requestIdleCallback 在空闲时段按文档顺序逐张后台预渲染（滚动中自动暂停、跳过视口上方避免滚动锚定抖动），同源码图表命中 LRU 缓存（SVG + 实测高度）免重复渲染且创建即预留精确高度——打开时长任务从 ~7s 降至 ~2s，预渲染不再引起布局位移，滚动到图表处通常已就绪
- **代码块纯文本占位**：CodeMirror 挂载前用同字体/行高/最大高度的 `<pre>` 占位显示源码，挂载前后高度差接近 0，打开大文档不再因逐块挂载产生布局跳变（窗口抖动）
- **大纲滚动采样零 DOM 开销**：当前章节跟踪用缓存标题位置 + scrollTop 二分比较（120ms 节流），替代 posAtCoords 采样（万行文档单次 50ms+），滚动路径无 JS 长任务；文档重灌（切 tab）后按恢复的滚动位置自动定位，无需手动滚动
- **查找面板防抖**：查找词 120ms 防抖，连续输入只触发一次全文匹配
- **滚轮监听器按需挂载**：`Ctrl/Cmd+滚轮` 缩放的 `passive:false` 监听器仅在 Ctrl/Cmd 按下时挂载，普通滚动时 window 上无任何 wheel 监听器走浏览器合成线程快速路径，修复万行文档滚轮失效问题（`useCtrlWheelZoom` hook）

### 快捷键体系
- 完整覆盖 Markdown 编辑常用快捷键（加粗、斜体、标题、列表、代码块等）
- 应用级快捷键：切换侧边栏、切换大纲、查找替换、偏好设置、快捷键帮助
- 按 `Ctrl/Cmd+/` 弹出快捷键帮助面板，集中查阅所有快捷键
- **快捷键自定义**：在帮助面板点击「自定义…」即可重新绑定应用级快捷键（含冲突检测、一键恢复默认）

## 技术栈

| 层 | 选型 |
|---|---|
| 应用外壳 | Tauri 2.x |
| 前端框架 | React 19 + TypeScript |
| 编辑器内核 | Milkdown（基于 ProseMirror） |
| 状态管理 | Zustand |
| 代码高亮 | CodeMirror 6 |
| 公式渲染 | KaTeX |
| 图表 | Mermaid |
| 文件系统 | Tauri 内置 `fs` / `dialog` API |

## 项目结构

```
src/
├── components/
│   ├── Editor/        # Milkdown 编辑器封装及插件（代码块、数学、Mermaid、图片、链接、表格、脚注、TOC、Front Matter、查找替换、专注模式、斜杠菜单、callout、错误边界等）
│   ├── GlobalSearch/  # 全局搜索面板
│   ├── QuickOpen/     # 快速打开面板（Ctrl/Cmd+P）
│   ├── Sidebar/       # 文件树侧边栏（含重命名/删除/新建、最近文件）
│   ├── Tabs/          # 多标签页栏（含右键菜单、拖拽重排）
│   ├── Outline/       # 大纲面板（标题跳转、当前高亮）
│   ├── Settings/      # 偏好设置面板
│   ├── Shortcuts/     # 快捷键帮助与自定义面板
│   └── StatusBar/     # 字数统计状态栏
├── lib/               # 文件读写、导出、大纲解析、字数统计、自动保存、文件监听、全局搜索封装
├── store/             # Zustand store（workspace / theme / settings / ui / shortcuts）
└── App.tsx
src-tauri/             # Rust 后端（Tauri 配置、文件系统命令、全局搜索命令、Pandoc 导出）
```

## 快捷键

按 `Ctrl/Cmd+/` 可在应用内随时查阅完整快捷键列表。常用快捷键：

| 快捷键 | 功能 |
|---|---|
| `Ctrl/Cmd+B` | 加粗 |
| `Ctrl/Cmd+I` | 斜体 |
| `Ctrl/Cmd+E` | 行内代码 |
| `Ctrl/Cmd+Alt+1 … 6` | 转为 H1 ~ H6 |
| `Ctrl/Cmd+Alt+C` | 代码块 |
| `Ctrl/Cmd+Alt+7 / 8` | 有序列表 / 无序列表 |
| `Ctrl/Cmd+Shift+B` | 引用块 |
| `Tab / Shift+Tab` | 缩进 / 提升列表项 |
| `Ctrl/Cmd+S` | 保存 |
| `Ctrl/Cmd+F` | 查找（当前文件） |
| `Ctrl/Cmd+R` | 替换（当前文件，自动展开替换框） |
| `Ctrl/Cmd+Shift+F` | 全局搜索（工作区所有文件） |
| `Ctrl/Cmd+P` | 快速打开文件（Quick Open） |
| `Ctrl/Cmd+\` | 切换侧边栏 |
| `Ctrl/Cmd+'` | 切换大纲面板 |
| `Ctrl/Cmd+,` | 偏好设置 |
| `Ctrl/Cmd+/` | 快捷键帮助 |
| `Ctrl/Cmd+滚轮` | 放大 / 缩小文档（50% ~ 300%） |
| `Ctrl/Cmd+0` | 重置缩放到 100% |
| `F11` | 禅模式（隐藏所有 UI） |
| `Esc` | 退出禅模式 |

> macOS 上 `Ctrl/Cmd` 对应 `⌘ Command`，`Alt` 对应 `⌥ Option`。

## 开发

```bash
# 安装依赖
pnpm install

# 前端开发（浏览器 mock 模式）
pnpm dev

# Tauri 桌面应用开发
pnpm tauri dev

# 构建生产包
pnpm build

# 打包桌面应用
pnpm tauri build

# 运行单元 + 组件测试（Vitest）
pnpm test

# 运行 E2E 测试（Playwright，首次需先 pnpm e2e:install 装 chromium）
pnpm e2e

# 性能 Benchmark：6 场景 × 档位，与基线自动比较判定回归
pnpm run benchmark
```

> 浏览器 `pnpm dev` 模式下会使用 mock 工作区，方便脱离 Tauri 环境调试 UI。
> GitHub Action 每次 push/tag 会自动跑全部测试，测试失败阻断构建。
> 性能 Benchmark 的判定规则（噪声门槛 3σ、判定分层、基线维护）见 [CONTRIBUTING.md](./CONTRIBUTING.md) 的「性能 Benchmark」章节。

## 版本记录

> 完整历史见 [CHANGELOG.md](./CHANGELOG.md)。

- **v3.3.0** Quick Open 交付完成（Epic #222 子项 1/2 #227 + 子项 2/2 #228，PR #240 / #242，另 #243 闭合计划 §7.1 清单）——**21 个提交 / 41 文件（+3707−213）**。①**忽略规则唯一真值源**（#227 / PR #240）：实测发现忽略黑名单有三份（`searchIgnore.ts` 14 项 / `search.rs` 同 14 项 / `commands/mod.rs` 5 项子集）而前端那份是死代码，且 **`.gitignore` 完全不生效**——新增 `commands/ignore_rules.rs` 收敛为三项穷举（默认 14 项目录 + 工作区内 `.gitignore`（含嵌套）+ 点号前缀隐藏项），明确不启用 `.ignore` / git 全局排除，`hidden(false)` 自行判点号以免连带过滤带 Windows 隐藏属性的普通文件；新增 `list_workspace_files` 命令（上限 50,000 + `truncated`、路径字节序升序、平台原生分隔符），**代次改由 Rust 侧分配**（`fetch_max` + 各 webview 独立计数器 = 先到窗口永久胜出、后开窗口被饿死，改为严格单调的 Rust 分配，前端零状态；`INDEX_GENERATION` 与 `SEARCH_GENERATION` 刻意分离以免打开面板取消在途搜索）；性能 1 万文件 1.543s → **52ms**、5 万 7.536s → **257ms**（~29×，线性）。②**Quick Open 面板**（#228 / PR #242）：缓存层懒构建 + TTL 30s + stale-while-revalidate（过期先出旧结果再后台重建）+ `invalidateEpoch` 拦「在途请求落定后写回」+ `refresh` 链递归消费 + `startBackgroundRebuild` 在 mint 处兜底防未处理拒绝；打分 `已打开(+1000) + 最近打开(0~100) + 匹配档位(0~100) − 深度×2`、排序键 code-unit 比较（**禁 `localeCompare`**）；面板渲染上限 200（**不做虚拟滚动**）、combobox a11y（焦点留输入框 + `aria-activedescendant`）+ 焦点陷阱 + **IME 组字守卫**；模态收敛为单个 `activeModal` / `requestModal` 唯一归并点并提到 `modalLayer`（修掉禅模式下的全局静默锁）、快捷键 `mod+p`（过滤 CM emacs `Ctrl-p`）；实测 5000 文件索引 **20.3ms**、打分 5000 候选 1.8ms（真机 DOM 耗时仍属人工核对项）。③**测试**：Vitest **130 文件 / 942 用例**、E2E **181**、Rust CI ubuntu 85 / windows 90（本机发版实测 90 通过 / 0 失败），变异验证 21 项全中。详见 `docs/v3.3.0 设计文档.md`
- **v3.2.0** 性能回归防线基建（#216，PR #231，17 个提交 / 50 文件 +6324−2）——**本版本不含产品代码改动**（`src/`、`src-tauri/` 零改动），全部为工程质量投入：新增 `pnpm run benchmark`（6 场景 × 4 档位 × 真实负载 fixture，median 与 p95 双行参与判定），建立三层判定——①**噪声门槛 3σ**（基线保留最近 8 次运行，按实测跨运行散布判定：`inputSyncMs` 3σ=1.25ms、`ttiMs` 3σ=241ms、而 vsync 量化的 `frameMs.p95` 仅 0.39ms，用固定百分比必然误判）、②**判定分层**（主指标可单独判 FAIL，派生指标需主指标佐证，小量级指标另有实测绝对地板）、③**两阶段复现**（首轮超阈值只复测该场景，仍超判 FAIL、回落判 WARN，退出码 0/1/2）；基线身份 = 测量配置（`<profile>/<mode>/r<rounds>/` 路径隔离，headless 日常回归与 uncapped 120fps 定向验证互不覆盖），滚动帧预算绝对目标仅在定向模式产出。新增 CI 工作流 `benchmark.yml`（PR 触发、不阻断合并）。单测 760→869 例，每个修复带变异验证；**09-16 重发（含 09-14~09-16 四轮评审加固：PR #232 / #235 / #237 / #239，关闭 #233 / #234 / #236 / #238）**：①**复测拆到独立 runner（#234）**——`benchmark` + `retest` 两个 job，同 runner 顺序复测过滤不掉「整台 runner 变慢」（两轮一起超阈值、穿过「连续 2 次」过滤），换 runner 后两轮才统计独立；无嫌疑的常规运行仍只有 1 个 job，另加 `PERF_SPLIT_RETEST` / `PERF_RETEST_ONLY` / `force_suspects` 便于确定性演练。②**会话标定负载（#236）**——每轮在页面内跑一段**与编辑器代码无关**的固定工作量（1500 节点 DOM 构建/样式/布局 + 2000 万步纯计算，实测 ≈36ms），进基线但**不参与判定**（机器慢不是代码回归），报告据此给**环境归因**：本次机器 vs 基线参考、是否超出历史范围（标定值分布即"机器档位"分布，双峰下 3σ 不适用），并做**两轮对账**——复测超范围→「复测环境异常，FAIL 未必成立」、首轮超范围而两台 runner 均复现→「回归在两台 runner 上复现」、无复测数据→明说"本次没有复测轮"；覆盖边界如实声明（CPU 与 DOM 构建/样式/布局，不含 IO/网络）。③**检出下限显式披露**（3σ ≥ 参考值 30% 的指标逐条列出，"行上的 PASS 不等于没问题"）与**整机漂移迹象披露**（判定不变，只披露）。④**判定覆盖口径修正**——只算相对行、空指标基线计入未覆盖、覆盖不足必须显式失败（不许"没比"伪装成"没回归"，也不许 `exit 1` 抢先掩盖）。⑤「判定分层」行名单改为从 `PRIMARY_METRICS` **派生**（原先手写，含一个不存在的 `inputMs`）。基线维护纪律写进 CONTRIBUTING：目标 8 点、每次发版后补 1 次、必须串行、不与 PR/发版检查并发（共享 runner 争用会让当次基准整体变慢）。质量门禁：Vitest **126 文件 869 用例**、E2E 171、Rust 59（CI 双平台全绿；本机 56 通过 + 3 个 symlink 用例受 Windows 开发者模式限制，非代码问题）、`tsc` 零错误。详见 `docs/v3.2.0 设计文档.md`
- **v3.1.0** 性能与测试可靠性修复——**2 个 issue（#212、#214）+ 2 个 PR，3 个代码提交 / 10 文件（+747−82）**：#212 万行复杂文档滚动掉帧（120Hz 大文档跌至 ~60fps）——把模式切换锚点采样从「滚动路径每帧 posAtCoords 强制布局」前移到「切换指令触发时同步采样」（新增 `lib/wysiwyg-anchor-sampler` 注册表，`setTabSourceMode` 翻转前采样，几何现场读可靠），滚动路径零几何读取；outline 标题重建与 CM6 懒挂载批次移入滚动停歇窗口，p99 尖刺消除。实测 60Hz median 16.7ms 满 vsync、超 2×median 帧数 0；#214 #136 门禁用例 B 首跑 flaky——fixture 退出过渡后改 `waitScrollConverged` 等双收敛（与用例 A 对齐），用例 B repeat-each=20 全过。全套 **116 单测文件 760 用例 / 171 E2E / 59 Rust** 全绿。详见 `docs/v3.1.0 设计文档.md`
- **v3.0.0** 主版本质量攻坚——**45 个 issue（#146–#188、#192、#200）+ 19 个 PR，61 个代码提交 / 113 文件（+8486−722）**，无新功能，全部投入数据安全、竞态治理、崩溃兜底、性能、安全与可访问性六大方向。三条贯穿原则：**失败要留下完整数据**（#146 写入回退改 4×50ms 原子重试、全败保留原件；#161 硬链接+删源闭合 TOCTOU 窗口、跨卷回退保留源原件）、**状态按最小粒度隔离**（#148/#149/#164 保存与退避从工作区级下沉到标签页/文件级）、**降级只针对真失败**（#172 慢启动只提示不卸载；#171 良性错误不再整页崩溃）。另含 #176 搜索预览字符级窗口防 OOM、#163 代次取消+分片并行、#168 Mermaid/KaTeX 动态导入（3.1MB 移出启动图）、#153 快照写入治理、#192 查找装饰增量、#162 asset scope 收缩与远程图 no-referrer、#188 五处键盘/ARIA 补齐。全套 **115 单测文件 745 用例 / 169 E2E / 59 Rust** 全绿。详见 `docs/v3.0.0 设计文档.md`
- **v2.8.1** 自身自动保存写盘不再被文件监听误判为外部修改（#144：三道防线 A/B/C 消歧义，82 单测 542 用例 / Build / 27 Rust 全绿）

## 贡献者

（按字母序）

- **Haoze Wu** ([@TomGoh](https://github.com/TomGoh)) — 本地图片相对路径解析、大型工作区按需加载与文件树优化、主题原生控件配色同步、Linux amd64 构建、多标签滚动/光标防串扰、macOS E2E 平台兼容（PR #8/#15/#16/#17/#18/#35/#37）；v3.0.0 中完成大纲标题扫描防抖与实时刷新、链接对话框主题样式与自定义配色、动态图片目录 ACL 权限，并补齐桌面端与持久化关键路径测试（9 个提交）
- **zhkp** ([@zhkp](https://github.com/zhkp)) — 项目作者，主要开发与维护

欢迎更多朋友参与贡献，详见 [CONTRIBUTING.md](./CONTRIBUTING.md)。

## License

[MIT](./LICENSE) © 2026 zhkp

